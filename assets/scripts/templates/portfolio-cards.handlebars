{{#each portfolio}}
  <div class="card">
    <div class="hover-text-container">
      <div class="card-image" style={{this.bgImage}}></div>
      <div class="card-copy hover-text hover-text-hidden">
        <p>{{this.desc}}</p>
      </div>
    </div>
    <a href={{this.link}}>
      <div class="card-header">
        <h4>{{this.title}}</h4>
      </div>
    </a>
  </div>
{{/each}}
